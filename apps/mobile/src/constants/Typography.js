import { COLORS } from './Colors';

export const FONTS = {
    title: {
      fontSize: 18,
      fontWeight: 'bold',
      color: COLORS?.text || '#000',
    },
    subtitle: {
      fontSize: 14,
      color: COLORS?.subtext || '#555',
    },
    label: {
      fontSize: 12,
      color: COLORS?.secondary || '#888',
    },
  };