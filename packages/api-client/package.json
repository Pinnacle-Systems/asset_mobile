{
  "name": "@repo/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.js",
  "exports": "./src/index.js",
  "scripts": {
    "build": "node -e \"console.log('Build step not required for JavaScript api client package yet')\"",
    "lint": "eslint src test --ext .js --config ../../tooling/eslint/node.js",
    "test": "vitest run",
    "clean": "rimraf dist build .turbo coverage"
  },
  "dependencies": {
    "@repo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "vitest": "^2.1.4"
  }
}
